[workspace]
resolver = "2"

members = [
    "crates/indexkit",
    "cli",
]

[workspace.package]
version = "1.0.1"
edition = "2021"
authors = ["userFRM"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/userFRM/indexkit"
documentation = "https://docs.rs/indexkit"

[workspace.dependencies]
# -- Core --
anyhow = "1"
thiserror = "2"

# -- Async runtime --
tokio = { version = "1", features = ["full"] }

# -- Serialization --
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# -- Logging --
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

# -- Time --
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"

# -- Async utilities --
async-trait = "0.1"

# -- HTTP --
reqwest = { version = "0.13", features = ["json", "rustls"], default-features = false }
bytes = "1"
futures = "0.3"

# -- Crypto --
sha2 = "0.11"

# -- Parquet / Arrow --
arrow = { version = "58", default-features = false, features = ["csv"] }
parquet = { version = "58", default-features = false, features = ["snap", "zstd", "arrow"] }

# -- XML --
quick-xml = "0.39"

# -- XLSX --
calamine = "0.34"

# -- CLI --
clap = { version = "4", features = ["derive", "env"] }

[profile.release]
lto = "thin"
codegen-units = 1
opt-level = 3
strip = true

[profile.dev]
opt-level = 0
