name: Release
on:
  push:
    tags: ["v[0-9]*"]
jobs:
  release:
    uses: kovagent/kit-ci/.github/workflows/release.yml@6ec005a7feb3df22c7011a50bdfefe324f37696f
    with:
      crates: "indexkit indexkit-cli"
    secrets:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
