name: Release
on: { push: { tags: ["v[0-9]*"] } }
jobs:
  release:
    uses: userFRM/kit-ci/.github/workflows/release.yml@main
    with: { crates: "indexkit indexkit-cli" }
    secrets: { CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }} }
